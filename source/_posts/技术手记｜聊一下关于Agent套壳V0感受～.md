---
title: 日常记录｜聊一下关于Agent套壳V0感受～
date: 2026-09-12 18:06:50
categories:
tags:
---
几个感受～
LLM放最后一层。不是先想「我接一个GPT」，然后让LLM去抗业务逻辑，最后搞一堆幻觉和不确定性，全都压在最核心的地方。
我这边的设计是：
- 事实层保证数据不会错
- 诊断引擎负责推理
- 权限卡住边界
- LLM做翻译官，把结构化的结论说成人话

这个设计的好处是：
1. LLM哪天抽风了，底下的东西照旧是对的，最多表述不好看，不会出业务事故。
2. 业务本身的边界场景够硬，适合用确定性架构来兜底，LLM只做最后一公里，不需要太强的模型，就是把结果说成人话，不需要花大价钱请贵模型念稿子。
3. 分层清楚，边界清晰，复杂度被正确的层消化掉，输入到诊断引擎这层的时候，数据已经是干净，结构化的了，诊断引擎的规则可以不用设计的太复杂。不需要搞一个Drools或者自己造一个规则引擎（而且V0版本直接做这些，太重）
4. 场景确定+分层清晰，规则分类器不需要太复杂，不需要一上来就做BERT做分类，然后花2周调模型，最后发现if-else就能搞定
5. 另，因为架构分层，所以LLM不扛业务逻辑，所以这一层是可替换的，今天用这家，明天那家降价了就换过去，底层的东西不用动。如果一开始就把业务逻辑塞进prompt里，换模型那就是给项目换根了。


再，一点私人观察：写了agentV0版本之后，发现agent听着很新，实际拆开了就是状态机，幂等，失败路径，异常处理之类的。居然跟我之前项目关键词类似～（笑）
