---
title: 空值缓存可以防止缓存穿透，但……什么时候要用它呢？
date: 2026-01-05 15:41:52
categories:
  - 技术手记
tags:
  - 缓存穿透
  - 空值缓存
  - Java
---
# 场景：
我写店铺列表接口，要不要上空值缓存呢？
key是固定的`cache:shop:types`

# 列表接口缓存空值，到底在防啥？
因为我们这边的key是固定的，所以不大可能会出现“不同id被人恶意传参穿透“的情况。

但它会遇到两种现实的问题：
## 场景1：数据真的为空（初始化/测试库/被误删/迁移期间）
比如：
- 新环境刚起，`shop_type`表还没有导入
- 测试库就是空的
- 生产迁移时短暂空窗
- 有人误删了类型表数据

这时候你如果不缓存空列表，会发生什么？
- 每一个请求都会miss（因为你不写缓存）
- 每一个请求都会去查DB
- DB这条SQL会被持续刷屏
这叫“空结果击穿/持续回源”（不算经典穿透，但效果类似：一直打库）

缓存空值，就能让系统在“确定为空”期间：
- 只打库一次
- 后续请求都直接返回空列表
- 等数据补上之后（或者删除缓存），再恢复正常
> 这在“配置/字典类数据”上很常见，你要允许系统在异常数据态下仍然稳定。

## 场景2：DB/Mapper间歇性返回空（抖动/超时降级/读写分离延迟）
有些情况会被认为「查不到就是空」，但可能是：
- 读库刚切换，延迟，短暂不可用
- 超时会被熔断/降级处理成空集合
- SQL会被限流，返回空（取决于代理/中间件）
- catch了异常，错误地返回`Collections.emptyList()`


如果这类偶发空，经常发生，而空值缓存TTL设置的很长，就会把偶发故障固化。

所以正确的姿势是：
- 空值缓存也可以做，但TTL要够短（分钟级）
- 让它只是短暂回血，而不是长期误导

# 那店铺列表真的需要空值缓存吗？
看业务约束。

如果你认为“店铺列表类型不可能为空，空就是故障”，那你可以：
- 不缓存空值
- 直接抛异常/返回错误码
- 同时报警

如果你希望“就算为空也别把DB打爆，系统先稳住“，那就：
- 缓存空列表，TTL分钟级（2～10分钟）
- 并且在后台日志里标记，店铺列表为空，供后续排查

# 总结
- 查询详情：防 穿透（不存在的id反复被查询）
- 查询列表：防 空结果持续回源（确实为空的时候反复被查）+ 防止故障抖动导致反复重建/打库
