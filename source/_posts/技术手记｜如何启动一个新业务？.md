---
title: 技术手记｜如何启动一个新业务？
date: 2026-04-26 21:03:02
categories:
  - 技术手记
tags:
  - 业务启动
  - 新业务冷启动
  - 业务建模
---

用途：快速理解新业务、识别核心对象与状态机、搭主流程、找 demo 与真实业务的差距，并形成可落地的实现顺序。

适用：支付、秒杀、登录、风控、核销、退款、通知、任务调度、第三方回调等业务模块。

<!-- more -->

## 0. 卡片元信息

- 业务名称：
- 所属模块：
- 创建日期：
- 当前阶段：未理解 / 理解中 / 已建模 / demo 可跑 / 可接业务 / 待补偿 / 待排障
- 优先级：P0 / P1 / P2
- 负责人：
- 相关接口 / 页面 / 文档：
- 相关表 / 缓存 / MQ / 第三方：

## 1. 一句话业务定义

### 1.1 这个业务到底在干嘛？

用一句人话说清楚，不要写技术词堆砌版。

模板句式：

> 该业务用于在【什么场景】下，为【谁】完成【什么动作】，并保证【哪些关键结果】成立。

填写：

- 场景：
- 目标用户 / 调用方：
- 核心动作：
- 系统必须保证的结果：

### 1.2 这个业务的“核心承诺”是什么？

这一步最重要。业务系统本质上是在维护承诺。

填写：

- 对用户的承诺：
- 对平台 / 商家的承诺：
- 对系统一致性的承诺：

## 2. 业务边界

### 2.1 这个业务“负责什么”

- 负责资格校验
- 负责资源占位
- 负责创建订单
- 负责支付发起
- 负责支付结果确认
- 负责权益发放
- 负责退款
- 负责核销
- 负责超时关闭
- 负责补偿
- 负责日志追踪
- 其他：

### 2.2 这个业务“不负责什么”

这一栏必须写，不然你会写着写着越界。

填写：

- 不负责：
- 依赖其他模块完成：
- 只接收结果、不处理过程的部分：

## 3. 核心对象识别

不先想表，先想“业务里到底有哪些对象在被系统管理”。

### 3.1 对象清单

| 对象 | 业务意义 | 谁创建 | 谁修改 | 是否需要状态机 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 用户 |  |  |  | 否 / 是 |  |
| 主业务对象 |  |  |  | 否 / 是 |  |
| 附属对象 |  |  |  | 否 / 是 |  |
| 事件 / 消息 |  |  |  | 否 / 是 |  |

### 3.2 主对象是谁？

每个业务通常都有一个最核心的“主对象”。

填写：

- 主对象：
- 为什么它是主对象：
- 其他对象围绕它做什么：

### 3.3 哪些对象只是“辅助信息”

填写：

- 静态配置类对象：
- 查询辅助类对象：
- 不需要独立生命周期的对象：

## 4. 事件时间线：先写电影，再写代码

先用“事件链”把散点串起来。不要一上来就画复杂流程图。

### 4.1 主成功时间线

按时间顺序写：

1. 
2. 
3. 
4. 
5. 
6. 
7. 
8. 

### 4.2 业务结束的标志是什么？

什么状态算成功闭环？

填写：

- 成功结束标志：
- 失败结束标志：
- 中间态有哪些：

### 4.3 谁触发了什么？

| 触发方 | 触发动作 | 系统响应 | 影响对象 |
| --- | --- | --- | --- |
| 用户 |  |  |  |
| 接口 |  |  |  |
| MQ 消费者 |  |  |  |
| 第三方回调 |  |  |  |
| 定时任务 |  |  |  |

## 5. 状态机卡片

不是所有对象都要复杂状态机。只给“生命周期复杂、会影响后续判断”的对象建状态机。

### 5.1 主对象状态机

- 对象名：
- 为什么需要状态机：

状态集合：

- INIT：
- PROCESSING：
- SUCCESS：
- FAIL：
- CLOSED / EXPIRED：
- 其他：

状态迁移表：

| 当前状态 | 触发事件 | 前置条件 | 下一状态 | 失败时怎么处理 | 是否可重试 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  | 是 / 否 |

非法迁移：

哪些迁移绝对不允许？这一步能防很多 bug。

填写：

- 不允许 A -> B，因为：
- 不允许 C -> D，因为：

### 5.2 附属对象状态机：有就写，没有就跳过

- 对象名：
- 状态集合：
- 迁移表：

| 当前状态 | 触发事件 | 下一状态 | 备注 |
| --- | --- | --- | --- |
|  |  |  |  |

## 6. 业务约束清单

真正决定代码结构的，不只是流程，而是约束。

### 6.1 唯一性约束

- 一人一单
- 一人一券
- 一次请求只能成功一次
- 同一业务 ID 只能处理一次
- 回调通知只能生效一次
- 其他：

补充说明：

### 6.2 时序约束

- 必须先发生：
- 不能提前发生：
- 可以异步发生：
- 必须最终一致的环节：

### 6.3 资源约束

- 库存是否可超卖：是 / 否
- 资源是否需要预占：是 / 否
- 占位超时是否释放：是 / 否
- 是否允许部分成功：是 / 否

### 6.4 权限 / 资格约束

- 谁能发起：
- 什么条件下允许：
- 什么条件下拒绝：
- 是否依赖登录态 / 风控 / 黑名单：

### 6.5 幂等约束

- 哪几个接口必须幂等：
- 哪几个消息消费必须幂等：
- 哪几个回调必须幂等：
- 幂等键是什么：

## 7. 读写矩阵：非常重要

把“流程图”翻译成“系统动作表”。

| 场景 / 动作 | 读哪些对象 | 写哪些对象 | 改谁的状态 | 发消息吗 | 调第三方吗 | 事务要求 |
| --- | --- | --- | --- | --- | --- | --- |
| 发起请求 |  |  |  | 是 / 否 | 是 / 否 |  |
| 异步处理 |  |  |  | 是 / 否 | 是 / 否 |  |
| 支付回调 |  |  |  | 是 / 否 | 是 / 否 |  |
| 超时关闭 |  |  |  | 是 / 否 | 是 / 否 |  |
| 退款 |  |  |  | 是 / 否 | 是 / 否 |  |

## 8. 主路径 / 异常路径拆分

### 8.1 主成功路径

用最短的话写清业务最顺的闭环。

1. 
2. 
3. 
4. 
5. 

### 8.2 可恢复失败路径

失败后还能靠重试 / 补偿救回来的。

| 失败点 | 现象 | 原因类型 | 处理策略 | 谁负责恢复 |
| --- | --- | --- | --- | --- |
|  |  | 系统波动 / 外部抖动 / 暂时失败 | 重试 / 补偿 / 延迟任务 |  |

### 8.3 不可恢复失败路径

直接失败，别假装能救。

| 失败点 | 现象 | 原因类型 | 是否重试 | 错误码 / 日志要求 |
| --- | --- | --- | --- | --- |
|  |  | 参数错 / 业务不成立 / 数据脏 / 越权 | 否 |  |

### 8.4 人工兜底场景

哪些不是代码自己能闭环的，必须留排障口子。

填写：

- 需要人工排查的场景：
- 必须留异常表 / 补偿表 / 原始消息记录的场景：
- 必须打 traceId 的场景：

## 9. demo -> 真实业务升级差距表

这一步专治“demo 能跑，但业务接不进去”。

| 维度 | demo 已有 | 业务还缺什么 | 优先级 |
| --- | --- | --- | --- |
| 主流程 |  |  | P0 / P1 / P2 |
| 状态机 |  |  | P0 / P1 / P2 |
| 幂等 |  |  | P0 / P1 / P2 |
| 并发控制 |  |  | P0 / P1 / P2 |
| 补偿机制 |  |  | P0 / P1 / P2 |
| 异常分类 |  |  | P0 / P1 / P2 |
| 日志 / 排障 |  |  | P0 / P1 / P2 |
| DTO / VO 契约 |  |  | P0 / P1 / P2 |
| 与现有模块接缝 |  |  | P0 / P1 / P2 |
| 配置化 |  |  | P0 / P1 / P2 |

### 9.1 当前 demo 只证明了什么？

填写：

- 已证明：
- 尚未证明：
- 最容易出事故的地方：

## 10. 接口契约清单

### 10.1 入参 DTO

- 字段：
- 必填项：
- 幂等字段：
- 来源字段：用户 ID、请求 ID、设备 ID 等
- 校验规则：

### 10.2 返回 VO

- 成功返回什么：
- 中间态返回什么：
- 失败返回什么：
- 前端最关心的字段：

### 10.3 错误码

| 错误码 | 含义 | 属于哪类失败 | 前端是否可提示 | 是否可重试 |
| --- | --- | --- | --- | --- |
|  |  |  | 是 / 否 | 是 / 否 |

## 11. 实现顺序：固定写，别脑子乱跳

你以后就按这个顺序写，不要一会儿想状态机，一会儿想 MQ，一会儿想 DTO。

### 11.1 第一阶段：先跑主路径

- 写一句话业务定义
- 列核心对象
- 写主成功时间线
- 定主对象状态机
- 先做 happy path 伪代码
- 跑通最小 demo

### 11.2 第二阶段：补业务约束

- 加资格校验
- 加唯一性约束
- 加时序约束
- 加状态合法性判断

### 11.3 第三阶段：补工程性

- 幂等
- 并发保护
- 异常分类
- 补偿机制
- 超时处理
- 原始消息 / 异常记录
- traceId / 关键日志

### 11.4 第四阶段：接入现有系统

- 统一 DTO / VO
- 统一错误码
- 接入风控 / 权限 / 审计
- 接入已有公共组件
- 配置外置
- 文档补齐

## 12. 伪代码骨架

在写 Java 前，先强迫自己写伪代码。

1. 获取上下文：用户 / 请求
2. 参数校验
3. 业务资格校验
4. 查询 / 锁定核心对象
5. 判断状态是否合法
6. 执行业务动作
7. 写入 / 更新核心对象
8. 必要时发消息 / 调第三方
9. 返回结果
10. 失败时分类处理：直接失败 / 可重试 / 补偿

本业务伪代码：

1. 
2. 
3. 
4. 
5. 
6. 
7. 
8. 

## 13. UML / 流程图产出要求

### 13.1 必画图

- 对象关系图
- 主路径流程图
- 主对象状态机图
- 异常 / 补偿流程图：复杂业务才画

### 13.2 画图标准

- 一个图只表达一层意思
- 不要把 happy path 和异常 path 画成一锅粥
- 状态机只画会流转的对象
- 流程图用“事件驱动”组织，不要只写方法名

## 14. 排障与观测设计

### 14.1 必须打日志的位置

- 请求入口：
- 状态变更前后：
- 调第三方前后：
- MQ 发送 / 消费：
- 失败分支：
- 补偿触发：

### 14.2 必须可查询的记录

- 主对象状态
- 附属对象状态
- 原始请求
- 原始消息
- 错误原因
- traceId
- 操作时间线

### 14.3 排障最小闭环

填写：

- 出问题时，我最先查什么：
- 第二步查什么：
- 哪些字段能串起全链路：

## 15. 风险审计

### 15.1 我目前最不懂的点

- 
- 
- 

### 15.2 当前最大风险

- 业务没理解透
- 状态机有缺口
- 幂等没想清
- 补偿没设计
- 并发风险高
- 第三方回调风险
- demo 和业务差距过大
- 其他：

### 15.3 需要先验证的点

- 
- 
- 

## 16. 最小交付清单

到什么程度，才算“这个业务不再是糊的”。

- 能用一句话说清业务目标
- 能列出核心对象
- 能写出主时间线
- 能给主对象画状态机
- 能区分主路径 / 异常路径
- 能说明哪些失败可重试，哪些不可
- 能说清 demo 已证明什么，没证明什么
- 能写出 service 层伪代码
- 能开始进代码而不再边写边猜

## 填写顺序建议

以后新业务来了，别从上到下机械填。按这个顺序最顺：

1. 一句话业务定义
2. 业务边界
3. 核心对象识别
4. 事件时间线
5. 状态机卡片
6. 业务约束清单
7. 主 / 异常路径拆分
8. demo -> 真实业务差距表
9. 接口契约
10. 实现顺序
11. 伪代码骨架
12. 最后补图、日志、风险审计

这个顺序的好处是：先把业务理解压实，再去想工程落地。不然很容易一边写代码一边猜业务，把自己写烦。

最实用的用法不是“每一栏都写很长”，而是：

- 第一次冷启动时：尽量填全
- 后面迭代时：只更新变动最大的那几栏
- 碰到 bug 时：直接回看状态机卡片、主 / 异常路径、读写矩阵
- 碰到新业务时：强迫自己先填一句话业务定义、对象、事件时间线
